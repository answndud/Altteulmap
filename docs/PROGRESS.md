# PROGRESS.md

기준일: 2026-04-27

## 문서 규칙
- 이 문서는 active 상태, blocker, 최근 검증, 다음 액션만 유지한다.
- 작업이 완료되면 이 문서의 최종 상태, 검증, 결과를 정리해 [COMPLETED.md](./COMPLETED.md)로 이동하고, active 문서에서는 바로 삭제한다.
- active 문서 상한:
  - 완료된 작업 0건 유지
  - 진행 중이거나 아직 끝나지 않은 작업만 남김
  - 작업이 모두 끝나면 완료 항목을 archive로 넘긴 뒤 active 본문은 전부 삭제
  - 모든 active 작업이 끝난 최종 상태는 `현재 active 작업 없음`만 남김

## Active 작업

Next.js to Vite + React migration is active.

## 현재 상태
- Phase 0 완료: API contract inventory와 Auth baseline을 `docs/migration-next-to-vite-react.md`에 기록함
- Phase 1 완료: 기존 Next production 경로를 보존한 Vite/Worker 병렬 스캐폴드와 SPA fallback 검증 완료
- Phase 2 마무리: Vite SPA의 `/`, `/place/:id`, `/submit`, `/report`, `/login`, `/signup`, `/bookmarks` route를 mock Worker API 기반으로 연결하고 route/API parity gap을 정리함
- 최신 `@cloudflare/vite-plugin@1.35.0`은 `wrangler@^4.87.0`을 요구하고, 해당 Wrangler는 Node 22 이상을 요구함
- 현재 프로젝트 Node 기준은 20.20.2이므로 Cloudflare plugin/Wrangler는 Node 20 호환 버전으로 고정
- 기존 Next production 스크립트는 유지하고 `dev:vite`, `cf:build:vite`, `preview:vite`, `deploy:vite`를 병렬 추가함
- `wrangler.jsonc`는 기존 OpenNext production 설정으로 보존하고, Vite 검증은 `wrangler.vite.jsonc`를 사용함
- Claude Code 리뷰 제약을 반영해 Phase 2 전 API/Auth baseline, SPA fallback, SEO 범위, admin 보안 테스트, 산출물 비교 기준을 명시함
- SPA fallback 검증 중 `/api/categories` scaffold가 기존 contract와 달라 `{ groups, categories }` 응답으로 수정함
- `/api/places/map`, `/api/places/:id`를 mock/query 기반 Worker API로 추가해 public route 이관의 데이터 경로를 열었음
- client bundle이 `places/queries` import 때문에 mock catalog를 포함해 1.7MB까지 커지는 문제를 발견하고, client route는 local `formatKrw` helper를 사용하도록 수정함
- `/submit`, `/report`는 기존 public form component를 재사용해 Vite route로 연결했고, Worker에 `/api/places`, `/api/reports` mock POST contract를 추가함
- `/login`, `/signup`은 기존 auth form의 Next 전용 import를 피하기 위해 Vite 전용 route form으로 추가함
- Worker에 Phase 4 전 임시 auth scaffold로 `/api/auth/csrf`, `/api/auth/session`, `/api/auth/providers`, `/api/auth/signin/:provider`, `/api/auth/callback/credentials`, `/api/auth/signup`을 추가함
- `/` route의 placeholder map panel을 기존 `NaverMapPanel`로 교체해 Naver SDK/fallback preview/marker selection 경로를 연결함
- `/place/:id` route에 가격 제보, 댓글, 반응 상호작용을 연결함
- `/bookmarks` route를 auth scaffold session 기준으로 연결함
- Phase 2 closure 기준으로 client route parity, Worker API parity, Phase 3 DB-backed API 이관 순서를 `docs/migration-next-to-vite-react.md`에 고정함
- Phase 3 시작:
  - `src/worker/db.ts` 추가: 기존 env 이름 `DATABASE_URL`, `USE_MOCK_DATA`를 Worker binding에서 읽는 DB client helper 추가
  - `src/worker/places-read-repository.ts` 추가: `GET /api/places/map`, `GET /api/places/:id`용 Worker-safe DB-backed read repository 추가
  - 기존 `features/places/repository.ts`는 `server-only`, admin moderation, Next 서버 경계가 섞여 있어 Worker entry에서 직접 import하지 않음
  - DB 비활성 또는 DB read 실패 시 기존 mock catalog fallback을 유지해 로컬/전환 중 smoke가 깨지지 않게 함
  - local scaffold session id는 UUID가 아니므로 DB `user_id` 비교에는 사용하지 않고, detail viewer 권한은 admin role/visitor id 중심으로 제한함
- Phase 3 public write 경계 시작:
  - `src/worker/public-write-actor.ts` 추가: Worker-safe visitor cookie, local session actor, rate-limit header helper를 분리함
  - `src/worker/places-write-repository.ts` 추가: 장소 등록, 가격 제보, 댓글, 댓글 삭제, 반응 DB write helper를 추가함
  - `src/worker/reports-write-repository.ts` 추가: 신고 DB write helper를 추가함
  - 기존 env 이름과 DB schema는 변경하지 않음
  - DB binding이 없는 로컬에서는 기존 mock fallback route가 계속 동작하도록 유지함
  - DB-backed route는 local scaffold session의 non-UUID user id를 DB user id로 쓰지 않고 visitor/anonymous write를 우선 사용함. 실제 Auth.js session user id 보존은 Phase 4 범위로 유지함
- Phase 3 telemetry 이관:
  - `src/worker/telemetry-repository.ts` 추가: `visit_activity` DB write, 30분 bucket dedupe, 120일 retention prune 로직을 Worker-safe로 분리함
  - `POST /api/telemetry/visit`를 Hono Worker route에 추가함
  - 기존 validation shape, visitor cookie, `ok/tracked/source` 응답 shape를 유지함
  - 공유 source validation은 `ref=share` 없이 `source`만 보내면 기존처럼 validation error를 반환함
- Phase 3 contract 비교 harness 추가:
  - `scripts/compare-vite-contract.mjs` 추가
  - `npm run migration:contract` 추가
  - 기본 비교 대상은 `CONTRACT_NEXT_BASE_URL`과 `CONTRACT_VITE_BASE_URL`
  - `CONTRACT_SKIP_NEXT=1`이면 Vite Worker contract만 검사함
  - `CONTRACT_EXPECT_VITE_SOURCE=database|mock`이면 Vite response의 `source`를 강제해 DB binding 검증이 mock fallback으로 조용히 통과하지 않게 함
  - read API, unauth bookmark, public write validation, telemetry validation/success shape를 검사함
  - 실제 DB write를 생성하는 성공 mutation은 기본 검사에서 제외해 운영 데이터 변경 위험을 줄임
- Phase 3 Next/Vite contract 비교 1차 실행:
  - Next dev는 `USE_MOCK_DATA=true npx next dev --webpack -p 3000`로 실행
  - Vite Worker는 `npm run cf:build:vite` 산출물을 `wrangler dev --port 3120 --local`로 실행
  - 최초 비교에서 `/api/places/map` Vite mock fallback이 preview가 아니라 detail item shape(`priceItems`, `history`, `comments`)를 반환하는 drift를 발견함
  - `src/worker/places-read-repository.ts`의 mock map fallback을 Next와 같은 preview shape로 좁혀 수정함
  - 재실행 결과 Next/Vite contract comparison 통과
- Phase 3 DB-backed smoke 시도:
  - Vite Worker에 `DATABASE_URL`/`USE_MOCK_DATA=false` binding을 주입해 `localhost:3121`에서 실행함
  - 현재 `.env.local`의 `DATABASE_URL`은 운영 Supabase가 아니라 `127.0.0.1:5432/altteulmap` 로컬 DB를 가리킴
  - 로컬 Postgres listener가 없어 Node 직접 접속은 `ECONNREFUSED 127.0.0.1:5432`로 실패함
  - Worker DB read도 `proxy request failed, cannot connect to the specified address` 후 mock fallback으로 내려감
  - `CONTRACT_EXPECT_VITE_SOURCE=database` guard가 의도대로 mock fallback을 실패 처리함
  - mock fallback 조건의 Next/Vite contract comparison은 계속 통과함
- Phase 3 DB-backed parity 완료:
  - `docker compose up -d postgres`, `npm run db:push`, `npm run db:seed`로 로컬 DB를 준비함
  - 로컬 DB active places `1000` 확인
  - Worker DB read/telemetry smoke에서 `source:"database"`, `mock:false`, telemetry `tracked:true` 확인
  - 첫 DB-backed write smoke 후 Cloudflare Worker의 request 간 I/O 재사용 제한 오류를 발견함
  - `src/worker/db.ts`, `src/worker/telemetry-repository.ts`, `src/worker/index.ts`를 수정해 DB 작업 후 Postgres client를 명시적으로 close하도록 정리함
  - `CONTRACT_EXPECT_VITE_SOURCE=database` 조건의 Next/Vite contract comparison 통과
  - 로컬 DB에서 가격 제보, 댓글, 반응, 신고, 댓글 삭제 successful write smoke 통과
  - successful write smoke 후 `npm run db:seed`를 다시 실행해 로컬 DB를 seed 기준 상태로 복구함
- Phase 4 credentials/session 배치:
  - `src/worker/auth-repository.ts` 추가
  - Worker credentials login을 DB-backed `users`/`auth_accounts`/`password_hash` 검증으로 교체함
  - demo/admin 계정의 env password override 의미 유지
  - `next-auth.session-token` cookie 이름을 유지하되 Worker signed payload `v1.<payload>.<signature>`로 저장함
  - `/api/auth/session`은 기존 shape `{ user:{ id, email, name, role }, expires }`를 유지함
  - `/api/auth/signup`을 DB-backed credentials signup으로 연결함
  - Worker auth DB 작업도 request 종료 후 Postgres client를 닫도록 구성함
  - Kakao/Naver OAuth redirect/callback/account sync는 다음 Phase 4 배치로 분리함
- Phase 4 OAuth redirect/callback 배치:
  - Worker에 `/api/auth/signin/kakao`, `/api/auth/signin/naver` authorization redirect 추가
  - Worker에 `/api/auth/callback/kakao`, `/api/auth/callback/naver` callback route 추가
  - `next-auth.state` signed state cookie와 query state 검증 추가
  - Kakao/Naver token exchange/profile fetch/account sync/session issue flow 추가
  - OAuth failure redirect 의미 유지: `OAuthCallback`, `OAuthEmailRequired`, `OAuthAccountSyncFailed`
  - synthetic account sync smoke로 `users`/`auth_accounts` DB sync 확인
  - 실제 provider live callback은 redirect URI 등록이 필요한 staging/production smoke로 남김
- Phase 5 admin API 통합 시작:
  - 현재 story는 `/api/admin/*` 서버 권한 경계와 DB-backed moderation endpoint 이관
  - UI route 보호는 다음 admin SPA story에서 다루고, 이번 story의 실제 보안 경계는 Worker route의 `requireAdmin` 검사로 둠
  - 기존 Next admin API의 401/403/400/404와 list/action response shape를 유지해야 함
- Phase 5 admin API 서버 경계 1차 완료:
  - `src/worker/admin-repository.ts` 추가
  - Worker entry에 `/api/admin/places`, `/api/admin/prices`, `/api/admin/reports`, `/api/admin/price-items/:id` 계열 route 추가
  - `requireAdminSession`으로 session 없음 `401`, 일반 사용자 `403`, admin 통과를 분리함
  - 기존 `server-only` admin/places repository를 Worker entry에 직접 import하지 않고 Worker-safe Drizzle 쿼리로 분리함
  - moderation suggestion import/persistence는 Worker admin 1차 범위에서 제외함
  - 로컬 DB smoke에서 pending 장소/가격 제보/신고를 만들고 admin 처리 성공 경로를 확인한 뒤 `npm run db:seed`로 복구함
- Phase 5 admin SPA 통합 시작:
  - 현재 story는 `/admin`, `/admin/places`, `/admin/prices`, `/admin/reports` Vite route 추가
  - Next 전용 admin page/component는 직접 import하지 않고 Vite 전용 fetch 기반 route로 구성함
  - UI route 접근 차단은 `/api/auth/session`과 `/api/admin/*` 응답을 사용한 UX 처리로 구현하고, 실제 보안 경계는 이미 추가한 `/api/admin/*` 서버 검사로 유지함
- Phase 5 admin SPA 1차 완료:
  - `/admin/*` placeholder를 React Router 기반 `AdminRoutes`로 교체함
  - `/admin`, `/admin/places`, `/admin/prices`, `/admin/reports`를 추가함
  - admin SPA는 `/api/auth/session`으로 비로그인/권한 부족 UX를 분기하고, 데이터/액션은 `/api/admin/*`만 사용함
  - 기존 Next admin page/component는 `next/link`, server page, `server-only` type 의존성이 있어 직접 import하지 않음
  - admin overview의 방문 지표/최근 가입자 전체 parity와 `/admin/prices/places/:id` 가격 항목 편집 화면은 다음 admin 세부 story로 남김
- Phase 6 Vite/Worker 배포 산출물 점검 시작:
  - production `wrangler.jsonc`와 기존 OpenNext split 배포는 아직 유지함
  - 현재 story는 `npm run cf:build:vite` 이후 generated Wrangler config와 worker/client asset 구조를 검사하는 `deploy:check:vite` 추가
  - Cloudflare production cutover, Workers Builds 설정 변경, 실제 deploy는 staging smoke/parity 이후 별도 단계로 유지함
- Phase 6 Vite/Worker 배포 산출물 점검 1차 완료:
  - `scripts/check-vite-worker-output.mjs` 추가
  - `npm run deploy:check:vite` 추가
  - generated Wrangler config가 Next/OpenNext output을 참조하지 않는지, SPA fallback assets 설정이 유지되는지 검사함
  - `docs/deploy/deploy-cloudflare.md`에 Vite migration 배포 검증 기준을 추가함
- Phase 5 admin 세부 route 보강 시작:
  - 현재 story는 `/admin/prices/places/:id` 가격 항목 관리 화면 추가
  - 기존 Worker API `GET /api/admin/prices/places/:id`, `PATCH /api/admin/price-items/:id`를 사용함
  - Next 전용 `AdminPriceItemForm`은 직접 import하지 않고 Vite 전용 작은 form으로 구현함
- Phase 5 admin 가격 관리 route 1차 완료:
  - `/admin/prices/places/:id` React Router route 추가
  - 장소별 가격 항목 목록, label/amount/unit/verification/representative/active 편집 UI 추가
  - 저장 액션은 `PATCH /api/admin/price-items/:id`를 사용함
  - admin session cookie를 직접 주입한 Playwright smoke에서 seeded place 가격 관리 화면 렌더링과 저장 버튼 노출을 확인함
  - admin overview의 방문 지표/최근 가입자 전체 parity는 Vite 1차 cutover blocking 요소가 아니라 후속 polish/parity 항목으로 남김
- Vite migration local smoke 자동화 시작:
  - 현재 story는 `npm run cf:build:vite` 산출물을 `wrangler dev`로 띄우고 public/auth/admin/API 핵심 경로를 자동 검증하는 스크립트 추가
  - live OAuth, Cloudflare Dashboard 설정 변경, production deploy는 포함하지 않음
- Vite migration local smoke 자동화 완료:
  - `scripts/smoke-vite-local.mjs` 추가
  - `npm run smoke:vite:local` 추가
  - script가 Vite build 후 generated Worker를 `wrangler dev --local`로 띄우고 public/auth/admin/API 핵심 경로를 검증함
  - smoke 첫 실행에서 admin API 병렬 요청 중 DB 연결 실패를 발견함
  - `src/worker/db.ts`를 `AsyncLocalStorage` 기반 request-local DB context로 바꿔 Worker request 간/동시 request 간 DB client 공유 위험을 줄임
  - `runWorkerDatabaseRoute(c.env, ...)`, `withWorkerDatabaseReadTimeout(env, ...)`로 DB client lifecycle을 요청 단위로 닫도록 정리함

## 최근 변경
- `docs/migration-next-to-vite-react.md` 생성
- `docs/blog/next-to-vite-migration-retrospective.md` 생성
- Vite/React Router/Hono/Cloudflare Vite plugin 의존성 추가 시작
- `vite.config.ts`는 ESM-only Cloudflare plugin 로딩 문제로 `vite.config.mts`로 전환
- Vite build output `dist/`를 git/lint ignore 대상에 추가
- `src/client`에 React Router 기반 placeholder route scaffold 추가
- `src/worker`에 Hono Worker scaffold와 `/api/health`, `/api/categories`, `robots.txt`, `manifest.webmanifest`, `sitemap.xml` 추가
- `wrangler.vite.jsonc`에 단일 Worker + SPA static assets 설정 추가
- `deploy:vite`가 root `wrangler.jsonc` 대신 generated Vite Wrangler config를 명시적으로 사용하도록 설정
- `docs/migration-next-to-vite-react.md`에 API contract inventory, Auth baseline, SPA fallback, SEO 범위, admin 보안 검증, 산출물 비교 기준 추가
- `src/worker/index.ts`의 `/api/categories` placeholder response를 기존 Next API contract와 맞춤
- `src/client/routes/MapRoute.tsx` 추가: React Router 기반 `/` route가 `/api/places/map`을 fetch해 검색/카테고리/목록을 렌더링
- `src/client/routes/PlaceDetailRoute.tsx` 추가: React Router 기반 `/place/:id` route가 `/api/places/:id`를 fetch해 상세/가격/코멘트를 렌더링
- `src/worker/index.ts`에 `/api/places/map`, `/api/places/:id` mock contract route 추가
- `/api/places/map` mock 응답은 기존 목록 제한 정책에 맞춰 returned items를 120개로 제한하고 `count`/`truncated`를 분리
- `src/client/routes/SubmitRoute.tsx` 추가: 기존 `PlaceSubmitForm` 재사용
- `src/client/routes/ReportRoute.tsx` 추가: 기존 `ReportSubmitForm` 재사용
- `src/worker/index.ts`에 `/api/places`, `/api/reports` POST 추가
- 공개 쓰기 mock POST 응답에도 `altteulmap_visitor_id` cookie를 `HttpOnly; SameSite=Lax; Path=/; Max-Age=31536000`로 설정
- `src/client/routes/LoginRoute.tsx` 추가: credentials login form과 기존 error copy를 Vite route로 이관
- `src/client/routes/SignupRoute.tsx` 추가: signup form과 기존 validation/failure copy를 Vite route로 이관
- `src/worker/index.ts` auth scaffold 추가:
  - `GET /api/auth/csrf`
  - `GET /api/auth/session`
  - `GET /api/auth/providers`
  - `GET /api/auth/signin/:provider`
  - `POST /api/auth/callback/credentials`
  - `POST /api/auth/signup`
- auth scaffold는 local demo/admin credentials와 baseline cookie/response shape 검증용이며, 실제 DB/OAuth 보존 이관은 Phase 4 범위로 유지
- `src/client/routes/MapRoute.tsx`에 `NaverMapPanel`을 직접 연결함
  - 기존 `MapExplorer` 전체는 북마크/session/상세시트 의존성이 섞여 있어 아직 가져오지 않음
  - marker click/list click은 Vite route state의 selected place로 동기화
  - selected place는 상세 route(`/place/:id`)로 이동 가능한 CTA를 노출
  - viewport refresh는 `/api/places/map`의 bounds query contract를 사용
- `vite.config.mts`에 기존 `NEXT_PUBLIC_NAVER_MAP_KEY_ID`, `NEXT_PUBLIC_NAVER_MAP_CLIENT_ID` env 이름을 Vite client build에서 사용할 수 있도록 define 추가
- `src/client/routes/PlaceDetailRoute.tsx`에 `PlacePriceReportForm`, `PlaceCommentsSection`, `RouteResetDetails`를 연결함
- 기존 `PlaceReactionButtons`는 `next/navigation`의 `router.refresh()` 의존성이 있어 Vite route에는 `src/client/components/VitePlaceReactionButtons.tsx`를 별도로 추가함
- `src/worker/index.ts`에 상세 상호작용 mock API 추가:
  - `POST /api/places/:id/prices`
  - `POST /api/places/:id/comments`
  - `DELETE /api/places/:id/comments/:commentId`
  - `PUT /api/places/:id/reaction`
- 댓글/반응 mock API는 `altteulmap_visitor_id` cookie 기준으로 현재 isolate 내 owner/reaction 상태를 추적함
- `src/client/routes/BookmarksRoute.tsx` 추가: `/api/bookmarks` contract를 먼저 읽고, 각 `/api/places/:id`로 상세 데이터를 합쳐 렌더링
- 기존 `BookmarkToggleButton`은 `next/navigation`의 `router.push/refresh()` 의존성이 있어 Vite route에는 `src/client/components/ViteBookmarkToggleButton.tsx`를 별도로 추가함
- `src/worker/index.ts`에 bookmark mock API 추가:
  - `GET /api/bookmarks`
  - `PUT /api/bookmarks/:id`
- bookmark mock API는 `next-auth.session-token` local session scaffold를 읽어 로그인 필요 `401`과 authenticated list/toggle contract를 유지함
- Phase 2 parity gap 정리:
  - `/admin/*`, `/api/admin/*`는 Phase 5 범위로 유지
  - OAuth/real session/signup은 Phase 4 범위로 유지
  - DB-backed public read/write API와 telemetry는 Phase 3 범위로 확정
  - Next/OpenNext/apps/admin 제거는 여전히 staging smoke/E2E parity 이후 Phase 7에서만 수행
- `src/worker/admin-repository.ts` 추가:
  - pending place list/detail/moderation DB helper
  - pending price report list/moderation DB helper
  - admin place price detail/price item update DB helper
  - content report list/status update DB helper
- `src/worker/index.ts`에 admin route 추가:
  - `GET /api/admin/places`
  - `GET /api/admin/places/:id`
  - `PATCH /api/admin/places/:id`
  - `GET /api/admin/prices`
  - `PATCH /api/admin/prices/:id`
  - `GET /api/admin/prices/places/:id`
  - `PATCH /api/admin/price-items/:id`
  - `GET /api/admin/reports`
  - `PATCH /api/admin/reports/:id`
- `docs/migration-next-to-vite-react.md`에 Vite Worker admin API status와 검증 결과 추가
- `src/client/routes/admin/AdminRoutes.tsx` 추가:
  - admin session guard
  - dashboard/places/prices/reports route
  - 장소 승인/반려, 가격 제보 승인/반려, 신고 상태 변경 UI
- `src/client/routes/admin/AdminRoutes.tsx`에 `/admin/prices/places/:id` route 추가
- `src/client/App.tsx`의 `/admin/*` placeholder를 `AdminRoutes`로 교체
- `docs/migration-next-to-vite-react.md`에 Vite admin SPA status와 검증 결과 추가
- `scripts/check-vite-worker-output.mjs` 추가
- `scripts/smoke-vite-local.mjs` 추가
- `package.json`에 `deploy:check:vite` 추가
- `package.json`에 `smoke:vite:local` 추가
- `src/worker/db.ts` request-local DB context로 변경
- `src/worker/index.ts`, `src/worker/places-read-repository.ts`의 DB route wrapper 호출 방식 갱신
- `docs/deploy/deploy-cloudflare.md`에 Vite/Worker 마이그레이션 배포 기준 추가

## 최근 검증
- `npm run typecheck` 통과
- `npm run cf:build:vite` 실패: `@cloudflare/vite-plugin` ESM-only package를 CommonJS config load 경로에서 resolve하려고 함
- `npm run cf:build:vite` 통과 after `vite.config.mts` 전환
- `npm run lint` 실패: `dist/` 산출물을 검사 대상으로 포함해서 generated bundle lint error 발생
- `npm run lint` 통과 after `dist/` ignore 추가
- `npm run typecheck` 통과
- `npm run cf:build:vite` 통과
- `git diff --check` 통과
- Next Auth baseline 확인: `GET /api/auth/csrf`, `GET /api/auth/session`, `POST /api/auth/callback/credentials`, `GET /api/auth/providers`, `GET /api/auth/signin/kakao`
- Next API baseline 샘플 확인: `/api/categories`, `/api/places/not-found`, `/api/bookmarks`, `/api/admin/places`
- Vite/Worker SPA fallback 확인:
  - `/api/health` -> `200 application/json`
  - `/api/categories` -> `200 application/json`
  - `/api/not-migrated-yet` -> `501 application/json`
  - `/sitemap.xml` -> `200 application/xml; charset=utf-8`
  - `/robots.txt` -> `200 text/plain; charset=utf-8`
  - `/manifest.webmanifest` -> `200 application/manifest+json; charset=utf-8`
  - `/place/test-route` -> `200 text/html; charset=utf-8`
- Vite route smoke 확인:
  - `/` -> `200 text/html`
  - `/api/places/map?query=김밥&scope=global` -> `200 application/json`
  - `/api/places/:id` -> `200 application/json`
  - `/place/:id` -> `200 text/html`
- `npm run cf:build:vite` 산출물:
  - Worker bundle `1,670.68 kB`, gzip `209.59 kB`
  - client JS `251.84 kB`, gzip `79.35 kB`
  - client CSS `51.13 kB`, gzip `10.05 kB`
- public write smoke 확인:
  - `/submit` -> `200 text/html`
  - `/report?placeId=test-place&placeName=Test%20Place` -> `200 text/html`
  - `POST /api/places` invalid body -> `400 application/json`, visitor cookie set
  - `POST /api/places` valid body -> `200 application/json`, preview 포함, visitor cookie 유지
  - `POST /api/reports` invalid body -> `400 application/json`, visitor cookie set
  - `POST /api/reports` valid body -> `200 application/json`, preview 포함, visitor cookie 유지
- `npm run cf:build:vite` 산출물 after public write route:
  - Worker bundle `1,799.94 kB`, gzip `235.17 kB`
  - client JS `366.11 kB`, gzip `112.58 kB`
  - client CSS `51.13 kB`, gzip `10.05 kB`
- `npm run lint` 통과
- `npm run typecheck` 통과
- `npm run cf:build:vite` 통과
- `git diff --check` 통과
- `npm run lint` 통과 after `/login`, `/signup`, auth scaffold 추가
- `npm run typecheck` 통과 after `/login`, `/signup`, auth scaffold 추가
- `npm run cf:build:vite` 통과 after auth scaffold 추가
- `npm run cf:build:vite` 산출물 after auth route:
  - Worker bundle `1,805.85 kB`, gzip `236.67 kB`
  - client JS `374.30 kB`, gzip `114.64 kB`
  - client CSS `51.13 kB`, gzip `10.05 kB`
- Vite auth smoke 확인:
  - `/login` -> `200 text/html`
  - `GET /api/auth/csrf` -> `200 application/json`, `next-auth.csrf-token`, `next-auth.callback-url` cookie set
  - `POST /api/auth/callback/credentials` valid local credentials -> `200 { url }`, session cookie set
  - `GET /api/auth/session` with session cookie -> `200 { user, expires }`
  - `POST /api/auth/callback/credentials` invalid credentials -> `401 { url:"...CredentialsSignin..." }`
  - `GET /api/auth/providers` -> `200 application/json`, credentials provider map
  - `GET /api/auth/signin/kakao?callbackUrl=/` -> `302 /login?callbackUrl=%2F&error=kakao`
  - `POST /api/auth/signup` invalid body -> `400 application/json`
  - `POST /api/auth/signup` valid local body -> `503 application/json`, DB 연결 없음 baseline 유지
- `npm run lint` 통과 after Naver map panel 연결
- `npm run typecheck` 통과 after Naver map panel 연결
- `npm run cf:build:vite` 통과 after Naver map panel 연결
- `npm run cf:build:vite` 산출물 after Naver map panel:
  - Worker bundle `1,805.85 kB`, gzip `236.67 kB`
  - client JS `397.45 kB`, gzip `121.33 kB`
  - client CSS `51.14 kB`, gzip `10.06 kB`
- Vite map smoke 확인:
  - `/` -> `200 text/html`
  - `/api/places/map?query=김밥&scope=global` -> `200 application/json`
  - `/api/places/map?minLat=37.4&maxLat=37.7&minLng=126.8&maxLng=127.2&zoom=13` -> `200 application/json`
  - SPA HTML root 포함 확인
- `npm run lint` 통과 after detail interaction 연결
- `npm run typecheck` 통과 after detail interaction 연결
- `npm run cf:build:vite` 통과 after detail interaction 연결
- `npm run cf:build:vite` 산출물 after detail interaction:
  - Worker bundle `1,814.17 kB`, gzip `238.02 kB`
  - client JS `411.23 kB`, gzip `124.13 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite detail interaction smoke 확인:
  - `/place/:id` -> `200 text/html`
  - `GET /api/places/:id` -> `200 application/json`
  - `POST /api/places/:id/prices` valid body -> `200 application/json`, visitor cookie set
  - `POST /api/places/:id/comments` valid body -> `200 application/json`, comment item 반환
  - `PUT /api/places/:id/reaction` like -> `200 application/json`, like count 반영
  - `DELETE /api/places/:id/comments/:commentId` with owner visitor cookie -> `200 application/json`
- `npm run lint` 통과 after bookmark route 연결
- `npm run typecheck` 통과 after bookmark route 연결
- `npm run cf:build:vite` 통과 after bookmark route 연결
- `npm run cf:build:vite` 산출물 after bookmark route:
  - Worker bundle `1,816.42 kB`, gzip `238.41 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite bookmark smoke 확인:
  - `/bookmarks` -> `200 text/html`
  - unauthenticated `GET /api/bookmarks` -> `401 { ok:false, message:"로그인이 필요합니다." }`
  - credentials login 후 `GET /api/bookmarks` -> `200 { items, count, source, userLabel, mock }`
  - authenticated `PUT /api/bookmarks/:id` bookmarked true -> `200 BookmarkToggleResult`
- `npm run typecheck` 통과 after admin API route/repository 추가
- `npm run lint` 통과 after admin API route/repository 추가
- `npm run cf:build:vite` 통과 after admin API route/repository 추가
- `npm run cf:build:vite` 산출물 after admin API:
  - Worker bundle `560.76 kB`, gzip `119.65 kB`
  - fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.89 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite admin API auth boundary smoke 확인:
  - unauthenticated `GET /api/admin/places` -> `401 { ok:false, message:"로그인이 필요합니다." }`
  - demo user session `GET /api/admin/places` -> `403 { ok:false, message:"운영자 권한이 필요합니다." }`
  - admin session `GET /api/admin/places` -> `200 { items, count, source:"database", mock:false }`
  - admin session `GET /api/admin/prices` -> `200 { items, count, source:"database", mock:false }`
  - admin session `GET /api/admin/reports` -> `200 { items, count, source:"database", mock:false }`
  - invalid `PATCH /api/admin/places/not-found` -> `400`
  - valid not-found `PATCH /api/admin/places/not-found` -> `404`
- Vite admin API successful mutation smoke 확인:
  - public `POST /api/places`, `POST /api/places/:id/prices`, `POST /api/reports`로 로컬 DB pending 데이터 생성
  - admin `PATCH /api/admin/places/:id` reject -> `200`
  - admin `PATCH /api/admin/prices/:id` reject -> `200`
  - admin `PATCH /api/admin/reports/:id` reviewing -> `200`
  - smoke 후 `npm run db:seed`로 로컬 DB seed 상태 복구
- `npm run typecheck` 통과 after admin SPA route 추가
- `npm run lint` 통과 after admin SPA route 추가
- `npm run cf:build:vite` 통과 after admin SPA route 추가
- `npm run cf:build:vite` 산출물 after admin SPA:
  - Worker bundle `560.76 kB`, gzip `119.65 kB`
  - fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `434.95 kB`, gzip `128.56 kB`
  - client CSS `51.36 kB`, gzip `10.09 kB`
- Playwright admin SPA smoke 확인:
  - unauthenticated `/admin` -> `로그인이 필요합니다`
  - demo user session `/admin` -> `운영자 권한이 필요합니다`
  - admin session `/admin` -> `운영 대시보드`
  - admin session `/admin/places` -> `신규 장소 승인 큐`
  - admin session `/admin/prices` -> `가격 제보 검토 큐`
  - admin session `/admin/reports` -> `신고 검토 큐`
- `npm run deploy:check:vite` 통과:
  - generated Wrangler config: `dist/altteulmap_vite_migration/wrangler.json`
  - Worker entry: `dist/altteulmap_vite_migration/index.mjs`, `560,765` bytes
  - client index: `dist/client/index.html`, `633` bytes
  - generated config에서 `.next`, `.open-next` 참조 없음
- `npm run typecheck` 통과 after `/admin/prices/places/:id` 추가
- `npm run lint` 통과 after `/admin/prices/places/:id` 추가
- `npm run cf:build:vite` 통과 after `/admin/prices/places/:id` 추가
- `npm run cf:build:vite` 산출물 after `/admin/prices/places/:id`:
  - Worker bundle `560.76 kB`, gzip `119.65 kB`
  - fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `439.50 kB`, gzip `129.21 kB`
  - client CSS `51.36 kB`, gzip `10.09 kB`
- Playwright admin price place route smoke 확인:
  - admin session cookie 주입 후 `/admin/prices/places/goodprice-157` -> `장소 가격 관리`
  - seeded place name `미림17분칼라` 렌더링 확인
  - 가격 항목 저장 버튼 노출 확인
- `npm run typecheck` 통과 after Vite local smoke/DB lifecycle 수정
- `npm run lint` 통과 after Vite local smoke/DB lifecycle 수정
- `npm run smoke:vite:local` 통과:
  - generated Worker local base URL `http://127.0.0.1:3130`
  - public API, telemetry, auth/admin API boundary, admin SPA route smoke 통과
  - Worker bundle `561.58 kB`, gzip `119.81 kB`
  - client JS `439.50 kB`, gzip `129.21 kB`

## 현재 blocker
- Phase 7 Next/OpenNext 제거와 production cutover는 로컬에서 계속 진행하면 안 됨
- 이유:
  - Kakao/Naver 실제 provider live callback은 배포 URL redirect URI 등록과 외부 provider login이 필요함
  - Cloudflare Workers Builds production 설정을 Vite 단일 Worker로 전환하려면 Dashboard build/deploy 설정 변경이 필요함
  - production cutover 전 staging smoke 또는 별도 preview Worker URL 확정이 필요함
  - Next/OpenNext/apps/admin 제거는 staging smoke와 기존 E2E parity 통과 후에만 수행한다는 제약이 있음

## 다음 액션
- 사용자가 Cloudflare/Vite staging 또는 preview Worker URL과 OAuth redirect URI 등록을 완료하면 live OAuth callback smoke를 진행
- staging smoke가 가능해지면 public/admin/API parity를 한 번 더 확인
- 그 이후 Phase 7에서 Next/OpenNext/apps/admin duplicate 제거 여부를 최종 결정
  - authenticated `GET /api/bookmarks` after add -> 추가한 place id 포함
  - authenticated `PUT /api/bookmarks/:id` bookmarked false -> `200 BookmarkToggleResult`
- `npm run lint` 통과 after Phase 2 closure docs
- `npm run typecheck` 통과 after Phase 2 closure docs
- 예정 검증:
  - `npm run typecheck`
  - `npm run lint`
  - `npm run cf:build:vite`
  - 로컬 Worker smoke: unauth `/api/admin/places` 401, user session 403, admin session 200, validation 400, not-found 404
  - `git diff --check`
- `git diff --check` 통과 after Phase 2 closure docs
- `npm run lint` 통과 after Phase 3 public read DB repository 추가
- `npm run typecheck` 통과 after Phase 3 public read DB repository 추가
- `npm run cf:build:vite` 통과 after Phase 3 public read DB repository 추가
- `npm run cf:build:vite` 산출물 after Phase 3 public read DB repository:
  - Worker entry `480.81 kB`, gzip `105.63 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite public read smoke 확인 on `localhost:3116`:
  - `/api/places/map?query=김밥&scope=global` -> `200 application/json`, `source:"mock"`, `items:55`, `count:55`, `X-Altteulmap-Map-Cache:bypass`
  - `/api/places/goodprice-14501` -> `200 application/json`, `source:"mock"`, price item 2개
  - `/api/places/not-found` -> `404 { error:{ code:"NOT_FOUND", message:"Place not found" } }`
- `npm run typecheck` 통과 after Phase 3 public write DB helper 추가
- `npm run lint` 통과 after Phase 3 public write DB helper 추가
- `npm run cf:build:vite` 통과 after Phase 3 public write DB helper 추가
- `npm run cf:build:vite` 산출물 after Phase 3 public write DB helper:
  - Worker entry `502.02 kB`, gzip `109.06 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite public write smoke 확인 on `localhost:3117`:
  - invalid `POST /api/places` -> validation 실패 `ok:false`
  - `POST /api/places/goodprice-14501/prices` -> `200`, `source:"mock"`, `mock:true`
  - `POST /api/places/goodprice-14501/comments` -> `200`, mock comment id 반환
  - `PUT /api/places/goodprice-14501/reaction` -> `200`, `reaction:"like"`
  - `DELETE /api/places/goodprice-14501/comments/:commentId` -> `200`
  - `POST /api/reports` -> `200`, `source:"mock"`, `mock:true`
- `git diff --check` 통과 after Phase 3 public write DB helper 추가
- `npm run typecheck` 통과 after telemetry Worker route 추가
- `npm run lint` 통과 after telemetry Worker route 추가
- `npm run cf:build:vite` 통과 after telemetry Worker route 추가
- `npm run cf:build:vite` 산출물 after telemetry Worker route:
  - Worker entry `507.26 kB`, gzip `110.39 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite telemetry smoke 확인 on `localhost:3118`:
  - valid `POST /api/telemetry/visit` with `{ path:"/place/goodprice-14501", ref:"share", source:"detail", scope:"public" }` -> `200 { ok:true, tracked:false, source:"mock" }`
  - ignored `/api` path payload -> `200 { ok:true, tracked:false, source:"mock" }`
  - invalid source without `ref=share` -> `400 { ok:false, message:"방문 이벤트 입력값 검증에 실패했습니다.", error }`
- `npm run typecheck` 통과 after contract harness 추가
- `npm run lint` 통과 after contract harness 추가
- `npm run cf:build:vite` 통과 after contract harness 추가
- `CONTRACT_SKIP_NEXT=1 CONTRACT_VITE_BASE_URL=http://localhost:3119 npm run migration:contract` 통과
  - categories `200`
  - map search `200`
  - place detail `200`
  - place not found `404`
  - bookmarks unauthenticated `401`
  - public write validation routes `400`
  - telemetry valid `200`
  - telemetry validation `400`
- `npm run typecheck` 통과 after map preview contract drift 수정
- `npm run lint` 통과 after map preview contract drift 수정
- `npm run cf:build:vite` 통과 after map preview contract drift 수정
- `npm run cf:build:vite` 산출물 after map preview contract drift 수정:
  - Worker entry `507.95 kB`, gzip `110.51 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- `CONTRACT_NEXT_BASE_URL=http://localhost:3000 CONTRACT_VITE_BASE_URL=http://localhost:3120 npm run migration:contract` 통과
  - categories `200`
  - map search `200`
  - place detail `200`
  - place not found `404`
  - bookmarks unauthenticated `401`
  - public write validation routes `400`
  - telemetry valid `200`
  - telemetry validation `400`
- DB-backed smoke 시도 on `localhost:3121`:
  - `GET /api/places/map?scope=global&query=김밥` -> `200`, `source:"mock"` because local DB unavailable
  - `GET /api/places/goodprice-14501` -> `200`, `source:"mock"` because local DB unavailable
  - valid `POST /api/telemetry/visit` -> `200 { ok:true, tracked:false, source:"mock" }`
  - invalid `POST /api/places` -> `400 { ok:false, message:"입력값 검증에 실패했습니다." }`
- `CONTRACT_SKIP_NEXT=1 CONTRACT_VITE_BASE_URL=http://localhost:3121 CONTRACT_EXPECT_VITE_SOURCE=database npm run migration:contract` 실패 as expected
  - failure: `map search expected Vite source 'database' but received 'mock'`
- `CONTRACT_NEXT_BASE_URL=http://localhost:3000 CONTRACT_VITE_BASE_URL=http://localhost:3121 CONTRACT_EXPECT_VITE_SOURCE=mock npm run migration:contract` 통과
- `npm run lint` 통과 after DB source guard 추가
- `npm run typecheck` 통과 after DB source guard 추가
- `npm run cf:build:vite` 통과 after DB source guard 추가
- `npm run cf:build:vite` 산출물 after DB source guard 추가:
  - Worker entry `507.95 kB`, gzip `110.51 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- `git diff --check` 통과 after DB source guard/docs update
- `docker compose up -d postgres` 통과
- `npm run db:push` 통과: no changes detected
- `npm run db:seed` 통과: Seed complete
- 로컬 DB 확인: active places `1000`
- DB-backed smoke on `localhost:3122`:
  - `GET /api/places/map?scope=global&query=김밥` -> `200`, `source:"database"`, `mock:false`, count `55`
  - `GET /api/places/:id` -> `200`, `source:"database"`, `mock:false`, price items 포함
  - valid `POST /api/telemetry/visit` -> `200 { ok:true, tracked:true, source:"database" }`
  - invalid `POST /api/places` -> `400`
- `CONTRACT_NEXT_BASE_URL=http://localhost:3000 CONTRACT_VITE_BASE_URL=http://localhost:3122 CONTRACT_EXPECT_VITE_SOURCE=database npm run migration:contract` 최초 실패
  - 원인: telemetry/write 성공 후 Postgres client가 request 간 재사용되어 Cloudflare Worker I/O lifecycle 오류 발생
  - error: `Cannot perform I/O on behalf of a different request`
- `npm run typecheck` 통과 after Worker DB client close 수정
- `npm run lint` 통과 after Worker DB client close 수정
- `npm run cf:build:vite` 통과 after Worker DB client close 수정
- `npm run cf:build:vite` 산출물 after Worker DB client close 수정:
  - Worker entry `508.41 kB`, gzip `110.58 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- `CONTRACT_NEXT_BASE_URL=http://localhost:3000 CONTRACT_VITE_BASE_URL=http://localhost:3122 CONTRACT_EXPECT_VITE_SOURCE=database npm run migration:contract` 통과
  - categories `200`
  - map search `200`
  - place detail `200`
  - place not found `404`
  - bookmarks unauthenticated `401`
  - public write validation routes `400`
  - telemetry valid `200`
  - telemetry validation `400`
- local DB successful write smoke 통과:
  - `POST /api/places/:id/prices` -> `200`, `source:"database"`, `ok:true`
  - `POST /api/places/:id/comments` -> `200`, `source:"database"`, `ok:true`
  - `PUT /api/places/:id/reaction` -> `200`, `source:"database"`, `ok:true`
  - `POST /api/reports` -> `200`, `source:"database"`, `ok:true`
  - `DELETE /api/places/:id/comments/:commentId` -> `200`, `source:"database"`, `ok:true`
- `npm run db:seed` 통과 after successful write smoke cleanup
- `npm run lint` 통과 after final Phase 3 DB-backed verification
- `npm run typecheck` 통과 after final Phase 3 DB-backed verification
- `git diff --check` 통과 after final Phase 3 DB-backed verification
- `npm run typecheck` 통과 after Phase 4 credentials/session batch
- `npm run lint` 통과 after Phase 4 credentials/session batch
- `npm run cf:build:vite` 통과 after Phase 4 credentials/session batch
- `npm run cf:build:vite` 산출물 after Phase 4 credentials/session batch:
  - Worker entry `516.15 kB`, gzip `112.65 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.88 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- Vite Worker auth smoke on `localhost:3123`:
  - `GET /api/auth/csrf` -> `200`, csrf token 있음
  - `GET /api/auth/providers` -> `200`, credentials + configured social provider keys
  - invalid credentials -> `401`, `CredentialsSignin` error URL
  - valid credentials `demo@altteulmap.local/demo1234` -> `200 { url:"/bookmarks" }`
  - session cookie starts with `v1.`
  - `GET /api/auth/session` -> DB user id, email, name, role 포함
  - authenticated `GET /api/bookmarks` -> `200`, session user label 유지
  - signup unique email -> `201 { ok:true, item }`
  - signup duplicate email -> `409 { ok:false }`
- `npm run db:seed` 통과 after Phase 4 signup smoke cleanup
- `npm run typecheck` 통과 after Phase 4 docs update
- `npm run lint` 통과 after Phase 4 docs update
- `git diff --check` 통과 after Phase 4 docs update
- `npm run typecheck` 통과 after Phase 4 OAuth route 추가
- `npm run lint` 통과 after Phase 4 OAuth route 추가
- `npm run cf:build:vite` 통과 after Phase 4 OAuth route 추가
- `npm run cf:build:vite` 산출물 after Phase 4 OAuth route 추가:
  - Worker entry `526.57 kB`, gzip `114.80 kB`
  - Worker fallback query chunk `1,614.04 kB`, gzip `192.93 kB`
  - client JS `418.82 kB`, gzip `125.89 kB`
  - client CSS `51.11 kB`, gzip `10.05 kB`
- OAuth route smoke on `localhost:3124`:
  - `GET /api/auth/providers` -> `200`, `credentials`, `kakao`, `naver`
  - `GET /api/auth/signin/kakao?callbackUrl=/bookmarks` -> `302`, host `kauth.kakao.com`, `response_type=code`, redirect URI `/api/auth/callback/kakao`, state cookie 있음
  - `GET /api/auth/signin/naver?callbackUrl=/` -> `302`, host `nid.naver.com`, `response_type=code`, redirect URI `/api/auth/callback/naver`, state 있음
  - missing code Kakao callback -> `/login?callbackUrl=%2F&error=OAuthCallback`
  - bad state Kakao callback -> `/login?callbackUrl=%2F&error=OAuthCallback`
  - synthetic `syncWorkerOAuthUser` smoke -> user/account sync 성공
- `npm run db:seed` 통과 after Phase 4 OAuth synthetic account cleanup
- `npm run typecheck` 통과 after Phase 4 OAuth docs update
- `npm run lint` 통과 after Phase 4 OAuth docs update
- `git diff --check` 통과 after Phase 4 OAuth docs update
- production 직접 cutover 전 최종 로컬 검증 통과:
  - `npm run typecheck` 통과
  - `npm run lint` 통과
  - `npm run cf:build:vite` 통과
  - `npm run deploy:check:vite` 통과
  - `npm run smoke:vite:local` 통과: `http://127.0.0.1:3130`, sample place `goodprice-157`
  - `git diff --check` 통과
